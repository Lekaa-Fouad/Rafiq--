/**
 * config.ts — App configuration
 *
 * IMPORTANT: Change BACKEND_URL to your computer's IP address.
 * Your phone and computer must be on the same WiFi network.
 *
 * To find your IP:
 *   Windows: open CMD → type "ipconfig" → look for IPv4 Address
 *   Mac/Linux: open Terminal → type "ifconfig" → look for inet
 */

// Your computer's IP address on the local WiFi network
export const BACKEND_URL = "http://192.168.100.8:8000";

// The API key (set in backend .env as API_KEY=123)
export const API_KEY = "123";
